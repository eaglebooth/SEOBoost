import { CampaignAction } from "@/components/CampaignAction";import { PageIntro } from "@/components/PageIntro";
export default async function Audit({params}:{params:Promise<{id:string}>}){const{id}=await params;return <main><PageIntro back={`/campaigns/${id}`} kicker="Function 03 / AI consensus" title="RUN THE WHITE-HAT JURY." copy="The verdict controls real escrow, so validators compare the substantive pay, review, or reject outcome."/><section className="content-band"><div className="shell workflow-layout"><aside className="workflow-guide"><span>Consensus checks</span><ol><li>Target rank independently proven.</li><li>Required hold duration independently proven.</li><li>Useful, expert content.</li><li>No material manipulation signals.</li></ol></aside><CampaignAction mode="audit" campaignId={id}/></div></section></main>}
