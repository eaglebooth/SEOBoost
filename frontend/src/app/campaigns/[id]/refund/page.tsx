import { CampaignAction } from "@/components/CampaignAction";import { PageIntro } from "@/components/PageIntro";
export default async function Refund({params}:{params:Promise<{id:string}>}){const{id}=await params;return <main><PageIntro back={`/campaigns/${id}`} kicker="Function 05 / client only" title="RECOVER REFUNDABLE ESCROW." copy="Only the immutable client can recover GEN after rejection or when the agency misses the locked evidence deadline."/><section className="content-band"><div className="shell workflow-layout"><aside className="workflow-guide"><span>Refund guards</span><ol><li>Caller must be the campaign client.</li><li>Status must be REJECTED, or evidence must be overdue.</li><li>Escrow and native balances must match.</li><li>REFUNDED campaigns cannot settle twice.</li></ol></aside><CampaignAction mode="refund" campaignId={id}/></div></section></main>}
