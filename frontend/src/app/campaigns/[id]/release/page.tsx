import { CampaignAction } from "@/components/CampaignAction";import { PageIntro } from "@/components/PageIntro";
export default async function Release({params}:{params:Promise<{id:string}>}){const{id}=await params;return <main><PageIntro back={`/campaigns/${id}`} kicker="Function 04 / value transfer" title="RELEASE APPROVED ESCROW." copy="The contract changes state before emitting the exact native GEN reward to the immutable agency address."/><section className="content-band"><div className="shell workflow-layout"><aside className="workflow-guide"><span>Release guards</span><ol><li>Campaign status must be APPROVED.</li><li>Escrow ledger must cover the reward.</li><li>Native contract balance must cover the reward.</li><li>PAID campaigns cannot settle twice.</li></ol></aside><CampaignAction mode="release" campaignId={id}/></div></section></main>}
